fastapi>=0.110
uvicorn[standard]>=0.27
pandas>=2.0
pydantic>=2.5
python-dotenv>=1.0
# AI providers are optional and imported lazily — install the one(s) you use.
# The app runs with only the selected provider's SDK present.
anthropic>=0.40
google-genai>=1.0
openai>=1.40
